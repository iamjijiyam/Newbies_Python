 # apply in list

nums = [10,32,78,24,61,53,92]

print("최소 :",min(nums)) # Min은 리스트 내 최솟값을 찾습니다. 

print("최대 :",max(nums)) # Max는 리스트 내 최댓값을 찾습니다.

print("합계 :",sum(nums)): # Sum은 리스트 내 모든 수의 합계를 구합니다

# 또한 리스트가 없을 시에도 해당 함수들을 사용할 수 있습니다.
print("리스트가없는 최소 :",min(9,12,4)) # 리스트가 없을 시 대신 최소, 최대, 합계를 구할 수들을 넣어주면 됩니다!

print("리스트가 없는 최대 :",max(9,12,4))

print("리스트가 없는 합계 :",sum([9,12,4])) 


